# 모바일 웹 플랫폼 배포 완벽 가이드
본 프로젝트는 **모바일(핸드폰) 화면**에 맞춰 최적화된 Web App (PWA 형태)으로 최종 제작되었습니다. 하지만 현재 만들어진 코드는 사용자님의 PC(로컬) 내에 존재하므로, 외부(핸드폰)에서 언제 어디서나 접속하기 위해서는 클라우드 플랫폼에 '배포(Deployment)'를 해야 합니다.

## 🚀 왜 배포가 필요한가요?
* 현재는 **본인의 PC**에서 명령어를 쳐야만 실행됩니다.
* 서버(클라우드)에 코드를 올려두면, **앱스토어에 앱을 올리듯 링크(URL) 하나만으로 언제든 핸드폰에서 웹브라우저로 접속**하여 실무에 즉각 사용할 수 있습니다.

---

## 🛠️ 가장 쉽고 무료인 배포 방법 (Streamlit Community Cloud)

현재 시스템은 파이썬과 Streamlit 기반으로 제작되었으므로, **Streamlit Cloud**를 이용하면 5분 만에 무료로 모바일 웹앱을 오픈할 수 있습니다.

### [1단계] GitHub에 소스 코드 올리기 (필수 준비)
Streamlit Cloud는 GitHub에 저장된 코드를 읽어서 실행합니다.
1. [GitHub](https://github.com/) 회원가입 및 로그인
2. 새로운 Repository(저장소) 생성 (예: `auction-rescue-app`)
3. `auction_rescue_mvp` 폴더 안에 있는 아래 핵심 파일들을 생성한 GitHub Repository에 업로드(Push) 합니다:
   - `app.py` (방금 모바일에 최적화한 메인 파일)
   - `analysis.py` (분석 엔진)
   - `report_generator.py` (PDF/PPT 구동 엔진)
   - `requirements.txt` (필요 파이썬 라이브러리 목록 - `streamlit`, `pandas`, `openpyxl`, `python-pptx`, `reportlab` 포함)
   - 기타 폰트 등 필요 파일 (`malgun.ttf` 등)

### [2단계] Streamlit Cloud에서 배포 버튼 클릭하기
1. [Streamlit Community Cloud](https://share.streamlit.io/)에 접속하여 GitHub 계정으로 로그인합니다.
2. 우측 상단의 **"New app"** (새로운 앱 만들기) 버튼을 클릭합니다.
3. [1단계]에서 만든 Repository를 선택합니다.
4. `Main file path` 입력란에 `app.py` 라고 지정합니다.
5. 앱의 접속 주소(Custom URL)를 지정할 수 있습니다. (예: `auction-rescue.streamlit.app`)
6. **"Deploy!"** 버튼을 클릭합니다.
7. 약 2~3분간 클라우드 서버가 코드를 읽고 세팅을 완료하면 화면에 앱이 켜집니다! 🎉

### [3단계] 스마트폰에서 진짜 '앱'처럼 사용하기 (PWA 적용형 설치)
클라우드 배포 후 발급받은 링크(예: `auction-rescue.streamlit.app`)를 핸드폰에서 처리하세요.

**아이폰 (Safari) 사용자:**
1. Safari에서 해당 링크 접속
2. 하단 중앙의 공유(내보내기) 버튼 클릭
3. **'홈 화면에 추가'** 클릭
4. 바탕화면에 일반 앱과 똑같은 아이콘 생성됨!

**안드로이드 (Chrome / Samsung Internet) 사용자:**
1. 스마트폰 브라우저에서 배포된 링크 접속
2. 우측 상단 메뉴(점 3개) 클릭
3. **'홈 화면에 추가'** 또는 **'앱 설치'** 클릭
4. 바탕화면에 아이콘 생성 완료!

---

## 📱 적용된 모바일 최적화 내용 (현재 `app.py` 수정본)
방금 업데이트된 `app.py`는 모바일 사용성(어고노믹스)에 기반해 제작되었습니다.

1. **중앙 집중형 UI 전환 (`layout="centered"`)**: 
   - 좌우로 낭비되는 여백을 제거하고 작고 좁은 휴대폰 뷰포트에 맞게 화면을 수직 정렬했습니다.
2. **모바일 탭(Tab) 뷰 도입**:
   - 기존의 방대한 데이터 테이블과 결과 리포트를 스크롤 없이 **스와이프형식의 Tab 구성**으로 분리하여 가독성을 높였습니다. 
3. **매크로 버튼 스타일링(CSS Inject)**:
   - "PDF 다운로드", "PPTX 다운로드", "파일 업로드" 영역의 버튼을 손가락 터치 타겟(Touch Target Requirement) 크기인 44px 이상으로 키우고 모서리를 둥글게 처리했습니다.
4. **불필요한 PC 메뉴 제거**:
   - PC 전용 샌드위치 메뉴, 헤더, 푸터를 숨겨서, 브라우저가 아닌 실물 애플리케이션(Native App)과 같은 깔끔함을 제공합니다.
5. **핵심 메트릭 뷰(KPI)**: 
   - 모바일 환경에서 가장 먼저 보여야 하는 정보(후보로 매칭된 갯수, 상위 매칭 정보 등)를 상단에 크게 고정 반영(Metrics Area)했습니다.